import * as Tape from "tape";

Tape("patch tests", (test: Tape.Test) => {
  test.test("default values to 0", (test: Tape.Test) => {
    test.test("default major version", (test: Tape.Test) => {

    });
  });
});
